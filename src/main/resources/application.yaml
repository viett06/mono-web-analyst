# ========================
# Database Configuration
# ========================
spring:
  datasource:
    url: jdbc:postgresql://localhost:5455/stock_analysis
    username: admin
    password: admin123

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true

  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB
jwt:
  signerKey: "nUfJ0IIEZWvOFqlud2hLrdVgGkYLYNH6JR6NkF6bAMq+yhdoQrah87JcbUsnXzQ4"
  valid-duration: 3600 #in second
  refreshable-duration: 36000 # in second
# ========================
# Server Configuration
# ========================
server:
  port: 8080

# ========================
# CORS Configuration
# ========================
cors:
  allowed-origins: "http://localhost:3000"
